import{DatabaseSync}from'node:sqlite';import{existsSync,mkdirSync,readdirSync,readFileSync,writeFileSync}from'node:fs';import{join}from'node:path';
const state='.wrangler/state/v3/d1/miniflare-D1DatabaseObject',file=existsSync(state)?readdirSync(state).find(x=>x.endsWith('.sqlite')&&x!=='metadata.sqlite'):null;if(!file)throw Error('로컬 TOSS D1 데이터가 없습니다');const db=new DatabaseSync(join(state,file),{readOnly:true}),raw=db.prepare("SELECT ticker,market_date date,high,low,close FROM daily_prices WHERE source='TOSS' ORDER BY ticker,market_date").all();db.close();
const history=Object.groupBy(raw,x=>x.ticker),source=JSON.parse(readFileSync('backtest/results/phase1_5-full.json','utf8')).records,records=new Map(source.map(x=>[`${x.ticker}|${x.date}`,x])),tickers=['AAPL','AMZN','AVGO','GOOG','META','MSFT','NVDA','TSLA'].filter(t=>history[t]?.length>504),avg=x=>x.length?x.reduce((a,b)=>a+b,0)/x.length:null,med=x=>{const a=[...x].sort((a,b)=>a-b),n=a.length;return n? n%2?a[(n-1)/2]:(a[n/2-1]+a[n/2])/2:null},valid=x=>typeof x==='number'&&Number.isFinite(x),clamp=(x,min,max)=>Math.max(min,Math.min(max,x));
const ma=(p,n)=>p.length<n?null:avg(p.slice(-n).map(x=>x.close)),annualMdd=(p,y)=>{let peak=-Infinity,draw=0;for(const x of p)if(x.date.startsWith(String(y))){peak=Math.max(peak,x.close);draw=Math.min(draw,x.close/peak-1)}return peak===-Infinity?null:draw},atr=p=>{const q=p.slice(-15);return q.length<15?null:avg(q.slice(1).map((x,i)=>Math.max(x.high-x.low,Math.abs(x.high-q[i].close),Math.abs(x.low-q[i].close))))},score=r=>Math.round(((r.mdd??0)+(r.relative??0)+(r.stability??0))/65*100),curveMdd=curve=>{let peak=curve[0]??1,draw=0;for(const x of curve){peak=Math.max(peak,x);draw=Math.min(draw,x/peak-1)}return draw};
function zone(p,type,mode){const last=p.at(-1);if(!last||p.length<252)return null;const price=last.close,recent=p.slice(-60),trail=p.slice(-252),years=[...new Set(p.map(x=>Number(x.date.slice(0,4))))].slice(-5),ref=med(years.map(y=>annualMdd(p,y)).filter(valid).map(Math.abs)),high52=Math.max(...trail.map(x=>x.high)),low52=Math.min(...trail.map(x=>x.low)),mas=[['ma60',ma(p,60)],['ma120',ma(p,120)],['ma200',ma(p,200)]],tol=clamp((atr(p)??price*.03)/price*1.25,.015,.08),raw=type==='buy'?[['swing60',Math.min(...recent.map(x=>x.low))],['low52',low52],['mdd',ref===null?null:high52*(1-ref)],...mas]:[['swing60',Math.max(...recent.map(x=>x.high))],['high52',high52],['ath',Math.max(...p.map(x=>x.close))],...mas.map(([n,v])=>[n,valid(v)&&v>price?v:null])],items=raw.filter(([,v])=>valid(v)).filter(([n,v])=>!(type==='buy'&&n.startsWith('ma')&&v>price)).map(([n,v])=>({n,v}));if(!items.length)return null;let best=[];for(const c of items){const group=items.filter(x=>Math.abs(x.v-c.v)/price<=tol);if(group.length>best.length)best=group}let values=best.map(x=>x.v);if(values.length<2){const eligible=items.filter(x=>type==='buy'?x.v<=price*1.15:x.v>=price*.85),near=[...(eligible.length?eligible:items)].sort((a,b)=>Math.abs(a.v-price)-Math.abs(b.v-price))[0];values=[near.v];const fallback=Math.max(near.v*.005,price*tol*.25);if(mode==='range')return{low:near.v-fallback,mid:near.v,high:near.v+fallback,width:fallback*2/near.v}}const mid=med(values);if(mode==='range'){const low=Math.min(...values),high=Math.max(...values);return{low,mid,high,width:(high-low)/mid}}const band=mode==='p15'?.015:.02;return{low:mid*(1-band),mid,high:mid*(1+band),width:band*2}}
function structure(mode){const rows=[];for(const ticker of tickers){const p=history[ticker];for(let i=252;i<p.length-252;i++){const q=p.slice(0,i+1),buy=zone(q,'buy',mode),sell=zone(q,'sell',mode);rows.push({ticker,price:p[i].close,buy,sell})}}const metrics=x=>{const buy=x.filter(r=>r.buy),sell=x.filter(r=>r.sell),both=x.filter(r=>r.buy&&r.sell);return{buyMeanWidth:avg(buy.map(r=>r.buy.width)),buyMedianWidth:med(buy.map(r=>r.buy.width)),sellMeanWidth:avg(sell.map(r=>r.sell.width)),sellMedianWidth:med(sell.map(r=>r.sell.width)),overlapRate:both.length?both.filter(r=>r.buy.high>=r.sell.low).length/both.length:null,buyAbovePriceRate:buy.length?buy.filter(r=>r.buy.high>r.price).length/buy.length:null,zoneCount:both.length}};return{metric:metrics(rows),byTicker:tickers.map(t=>({ticker:t,...metrics(rows.filter(r=>r.ticker===t))}))}}
function simulate(mode){const trades=[],curves={};for(const ticker of tickers){const p=history[ticker],start=Math.max(252,p.findIndex(x=>records.has(`${ticker}|${x.date}`))),end=p.length-253;let cash=1,shares=0,pos=null,curve=[];for(let i=start;i<=end;i++){const day=p[i],record=records.get(`${ticker}|${day.date}`);if(!pos&&record&&score(record)>=70){const buy=zone(p.slice(0,i+1),'buy',mode),sell=zone(p.slice(0,i+1),'sell',mode);if(buy&&sell&&sell.mid>buy.mid&&day.low<=buy.high&&day.high>=buy.low){shares=cash*.9995/buy.mid;cash=0;pos={i,entry:buy.mid,target:sell.mid,low:day.low,high:day.high}}}else if(pos){pos.low=Math.min(pos.low,day.low);pos.high=Math.max(pos.high,day.high);if(i>pos.i&&day.low<=pos.target&&day.high>=pos.target){cash=shares*pos.target*.9995;trades.push({ticker,closed:true,days:i-pos.i,net:pos.target/pos.entry*.999**1-1,mae:Math.min(0,pos.low/pos.entry-1),mfe:pos.high/pos.entry-1});shares=0;pos=null}}curve.push(cash+shares*day.close)}if(pos){const last=p[end],value=cash+shares*last.close;trades.push({ticker,closed:false,days:end-pos.i,net:last.close/pos.entry*.9995-1,mae:Math.min(0,pos.low/pos.entry-1),mfe:pos.high/pos.entry-1});curve[curve.length-1]=value}curves[ticker]=curve}const summarize=(list,curve)=>{const done=list.filter(x=>x.closed);return{entries:list.length,closed:done.length,arrivalRate:list.length?done.length/list.length:null,medianReturn:med(done.map(x=>x.net)),cumulative:curve.at(-1)-1,mdd:curveMdd(curve),meanMae:avg(done.map(x=>x.mae)),meanMfe:avg(done.map(x=>x.mfe)),medianDays:med(done.map(x=>x.days)),openRate:list.length?(list.length-done.length)/list.length:null}},curve=Array.from({length:Math.max(...Object.values(curves).map(x=>x.length))},(_,i)=>avg(Object.values(curves).map(x=>x[Math.min(i,x.length-1)]))),overall=summarize(trades,curve),perTicker=tickers.map(t=>({ticker:t,...summarize(trades.filter(x=>x.ticker===t),curves[t])}));return{overall,perTicker}}
function evaluate(mode){const s=structure(mode),p=simulate(mode);return{...s.metric,...p.overall,perTicker:s.byTicker.map(x=>({...x,...p.perTicker.find(y=>y.ticker===x.ticker)}))}}
const modes=[['range','min~max'],['p15','대표가격 ±1.5%'],['p20','대표가격 ±2.0%']],results=Object.fromEntries(modes.map(([key])=>[key,evaluate(key)])),rows=modes.map(([key,label])=>({label,...results[key]})),date=new Date().toISOString().slice(0,10),runId=`phase3-8-${date}`,sections=[['width','가격대 폭 비교',rows],['fixed','Fixed 운용성과',rows],['ticker','종목별 결과',modes.flatMap(([key,label])=>results[key].perTicker.map(x=>({label:`${label} · ${x.ticker}`,...x})))],['conclusion','최종 판단',rows.map(x=>({label:x.label,buyMedianWidth:x.buyMedianWidth,sellMedianWidth:x.sellMedianWidth,overlapRate:x.overlapRate,medianReturn:x.medianReturn,arrivalRate:x.arrivalRate,mdd:x.mdd,meanMae:x.meanMae}))]].map(([id,title,data],i)=>({id,parentId:null,sectionType:'report',title,summary:'MA 방향성 고정 · 실제 TOSS 일봉 독립 재시뮬레이션',reportData:{rows:data},sortOrder:i+1}));
const run={id:runId,runDate:date,phase:'3.8차 · 핵심가격대 폭 정밀화 검증',title:'MA 방향성 기반 핵심가격대 폭·운용성과 검증',algorithmVersion:'BT-v3.8',periodStart:'2022-01-25',periodEnd:'2025-08-21',universe:'TOSS 8종목 동일 스냅샷',config:{maDirection:true,atrCap:.08,atrMultiplier:1.25,minTolerance:.015,scoreThreshold:70,weights:'30/20/15',entry:'buy midpoint touch',exit:'entry-day fixed sell midpoint',warmup:252,lookAheadGuard:true},summary:results,conclusion:'MA 방향성 규칙을 고정하고 가격대 표시 폭만 바꾼 독립 재시뮬레이션입니다. 운영 앱 값은 자동 변경하지 않습니다.',createdAt:new Date().toISOString(),sections};
mkdirSync('backtest/results',{recursive:true});writeFileSync('backtest/results/phase3_8-summary.json',JSON.stringify(run,null,2));writeFileSync('backtest/results/phase3_8-full.json',JSON.stringify(results,null,2));for(const[name,data]of Object.entries({widths:rows,per_ticker:sections[2].reportData.rows}))writeFileSync(`backtest/results/phase3_8-${name}.json`,JSON.stringify(data,null,2));const stored={...run,sections:sections.map(s=>({...s,id:`${runId}-${s.id}`}))};let storage='local-only';if(!process.env.BACKTEST_LOCAL_ONLY&&process.env.MARKET_SYNC_URL&&process.env.MARKET_SYNC_TOKEN){const u=process.env.MARKET_SYNC_URL.replace(/\/api\/market-data.*$/,'/api/backtests'),r=await fetch(u,{method:'POST',headers:{Authorization:`Bearer ${process.env.MARKET_SYNC_TOKEN}`,'Content-Type':'application/json',...(process.env.SITES_BYPASS_TOKEN?{'OAI-Sites-Authorization':`Bearer ${process.env.SITES_BYPASS_TOKEN}`}:{})},body:JSON.stringify(stored)});if(!r.ok)throw Error(`D1 ${r.status}: ${await r.text()}`);storage='D1 saved'}console.log(JSON.stringify({storage,...results},null,2));
